-- Migration 001: Initial Schema
-- This migration creates the initial database schema for KubeChat
-- Date: 2025-01-10
-- Description: Creates core tables for users, clusters, queries, and audit logging

-- This migration is a placeholder as the schema is created in init.sql
-- Future migrations will go here for schema updates

-- Example migration structure:
-- ALTER TABLE users ADD COLUMN new_field VARCHAR(255);
-- CREATE INDEX idx_new_field ON users(new_field);

-- For now, this migration does nothing as init.sql handles the initial schema
SELECT 'Initial schema migration placeholder' AS message;