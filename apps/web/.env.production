# Production Environment Configuration for Kubernetes
# These values should be set by Kubernetes deployment manifests

# Backend API Service URL (set by K8s ConfigMap or Secret)
# API_URL will be injected by Kubernetes deployment manifests

# Environment
ENVIRONMENT=production
NODE_ENV=production

# Next.js Configuration
NEXT_TELEMETRY_DISABLED=1

# Feature Flags (controlled by K8s ConfigMap)
ENABLE_DEBUG=${ENABLE_DEBUG:-false}
ENABLE_ONBOARDING=${ENABLE_ONBOARDING:-true}
ENABLE_HELP_SYSTEM=${ENABLE_HELP_SYSTEM:-true}
ENABLE_GUIDED_TOURS=${ENABLE_GUIDED_TOURS:-true}

# Health Check
HEALTH_CHECK_ENABLED=${HEALTH_CHECK_ENABLED:-true}