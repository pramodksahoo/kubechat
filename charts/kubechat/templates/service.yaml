apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubechat.fullname" . }}
  labels:
    {{- include "kubechat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ include "kubechat.listenPort" . | int }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "kubechat.selectorLabels" . | nindent 4 }}
